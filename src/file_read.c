#include <rt.h>

char		*read_file(int fd)
{
	t_buff	*list_buf;
	ssize_t	;
	char	*file;


	//fonction de lecture
	// enregistrer dans le buff
	// si < à 4096
	//fonction de concaténation de structure vers char*
	//nettoyer buff

	// return (char *);
}