#include <libft.h>
#include <struct.h>

int		main(void)
{
	t_img		img;
	t_map		map;




/*
	init_window(200, 200);
	clear_image();
	copy_image_to_window();
	SDL_Delay(500);
	clear_image();
	SDL_Delay(500);
	free_resources();
*/

	return (0);
}
