#include <libft.h>
#include <struct.h>

/*
	Gestion input
	Lexeur 
	parseur
	Appel partie matthieu
*/

int		input(int argc, char **argv)
{
	if (argc == 1)
	{

	}
	else
}

int		main(int argc, char **argv)
{
	t_img		img;
	t_map		map;


	if (argc == 1)


/*
	init_window(200, 200);
	clear_image();
	copy_image_to_window();
	SDL_Delay(500);
	clear_image();
	SDL_Delay(500);
	free_resources();
*/

	return (0);
}
